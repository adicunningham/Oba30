@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<Oba30.Infrastructure.Objects.Tag> tags)
{
    foreach (var tag in tags)
    {
        <div class="tags-div">
            @Oba30.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
    }
}