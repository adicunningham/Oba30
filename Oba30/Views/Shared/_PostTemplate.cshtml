@model Oba30.Infrastructure.Core.Post

<div class="post">
    <div class="post-meta">
        <div class="row">
            
            <!-- Heading -->
            <div class="post-title">
                <h2>@Html.PostLink(Model)</h2>
            </div>
            
            <div class="row">
                
                <!-- Category -->
                <div class="post-category">
                    <span>Category: </span>@Html.CategoryLink(Model.Category)
                </div>
            
                <!-- Tags -->
                <div class="post-tags">
                    <span>Tags: </span>@BrowserHelpers.Tags(Html, Model.Tags)
                </div>
            
                <!-- Posted Date -->
                <div class="posted-date">
                    @Model.PostedOn.ToConfigLocalTime()
                </div>

            </div>

        </div>

        <!-- Short Description -->
        <div class="post-body">
            @Model.Description
        </div>
        
        <div class="post-foot">
            @Html.ActionLink("continue...", "post", "Blog",
            new
            {
                Year = Model.PostedOn.Year,
                Month = Model.PostedOn.Month,
                Day = Model.PostedOn.Day,
                Title = Model.UrlSlug
            },
            new { Title = "continue..."}
            )
        </div>
        .
    </div>
</div>